const http=require('http'),fs=require('fs'),path=require('path');const port=Number(process.env.PORT||3000),host=process.env.HOST||'127.0.0.1',publicDir=path.join(__dirname,'public');http.createServer((req,res)=>{const file=req.url==='/'?'/index.html':req.url.split('?')[0];const api=req.url.split('?')[0];if(api==='/api/config')return json(res,{mode:'sandbox',hasSandboxKey:false,hasProductionKey:false,commissionRate:.12});if(api==='/api/search')return json(res,{source:'sample',hotels:[{id:'mock-harbor-view',name:'Harbor View Suites',city:'Dubai',country:'AE',rating:4.8,price:228,image:'https://images.unsplash.com/photo-1566073771259-6a8506099945?auto=format&fit=crop&w=1200&q=80',address:'Dubai Marina, waterfront district',amenities:['Sea view','Breakfast','Pool','Airport transfer'],room:'Deluxe king room'}]});if(api==='/api/prebook')return json(res,{prebookId:'PB-DEMO',total:260});if(api==='/api/bookings')return json(res,{bookings:[],summary:{count:0,revenue:0,commission:0,confirmed:0}});const fp=path.join(publicDir,file);if(!fp.startsWith(publicDir)||!fs.existsSync(fp))return res.end('Not found');fs.createReadStream(fp).pipe(res)}).listen(port,host,()=>console.log(`Hotel portal running at http://${host}:${port}`));function json(res,p){res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify(p))}
